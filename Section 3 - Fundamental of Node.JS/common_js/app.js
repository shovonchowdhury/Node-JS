const {sum,product}=require('./math');
//require is a function which takes a path as an argument and returns module.exports from the path file. return type can be any type of value like object,num,string,array etc.

console.log(sum(1,2,3,4));
console.log(product(1,2,3,4));